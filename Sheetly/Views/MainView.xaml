<Window x:Class="Sheetly.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:uc="clr-namespace:Sheetly.UserControls"
        xmlns:views="clr-namespace:Sheetly.Views"
        xmlns:vc="clr-namespace:Sheetly.ValueConverters"
        mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="800"
      Title="Sheetly - Main">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- <Button Grid.Row="0" Grid.Column="1" Content="Add New File" Click="NewFileWindow"  />-->
        <StackPanel
            x:Name="FileDropStackPanel"
            Grid.Row="0"
            Grid.Column="1"
            AllowDrop="True"
            Background="White"
            Drop="FileDropStackPanel_Drop">
            <Label HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Consolas" FontWeight="Bold" FontSize="15" Content="Drop File Here"/>
        </StackPanel>
        <Button Grid.Row="0" Grid.Column="2" Content="Select File Here" Command="{Binding finderUpload}" Foreground="White"
                Background="#FF78AFFF" Padding="5" FontFamily="Consolas" FontWeight="Bold" FontSize="15" Margin="5" />

        <ItemsControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding FileList}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <uc:FileUserControl />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</Window>
